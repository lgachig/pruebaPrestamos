class Equipment {
    constructor(id, name, total, available) {
      this.id = id;
      this.name = name;
      this.total = total;
      this.available = available;
    }
  }
  
  module.exports = Equipment;